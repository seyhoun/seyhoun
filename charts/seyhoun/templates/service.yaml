apiVersion: v1
kind: Service
metadata:
  name: {{ include "seyhoun.fullname" . }}
  labels:
    {{- include "seyhoun.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "seyhoun.selectorLabels" . | nindent 4 }}
